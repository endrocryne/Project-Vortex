---
trigger: model_decision
description: When asked to update a configuration parameter, such as exposing a variable or parameter to the user
---

1) update as en entry in the GUI in the appropriate tab, and update the CLI code to accept this parameter, then update the code that saves and loads json configs to also have this as an entry.